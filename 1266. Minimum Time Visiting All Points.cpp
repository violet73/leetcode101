#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

class Solution {
public:
    int minTimeToVisitAllPoints(vector<vector<int>>& points) {
        // the min distance is first find min(|x1-x2|, |y1-y2|), 
        // than min distance = min(|x1-x2|, |y1-y2|) + |y1-y2| - |x1-x2|
    }
};

int main() {
    


    return 0;
}